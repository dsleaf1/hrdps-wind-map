# hrdps-wind-map

Static deploy of the BC-coast HRDPS wind map (true ~2.5 km via the hrdps-ingest pipeline → Cloudflare R2). Single self-contained page; coastline inlined; geotiff.js only for the GeoMet fallback.
